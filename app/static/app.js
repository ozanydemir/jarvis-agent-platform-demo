const photoStyles=document.createElement("link");photoStyles.rel="stylesheet";photoStyles.href="/static/photos.css";document.head.append(photoStyles);
const form=document.querySelector("#assistant-form"),message=document.querySelector("#message"),runButton=document.querySelector(".run");
function renderTrace(trace){const fragment=document.createDocumentFragment();trace.forEach((step,index)=>{const row=document.createElement("li"),number=document.createElement("span"),detail=document.createElement("div"),stage=document.createElement("strong"),summary=document.createElement("small"),status=document.createElement("b");number.textContent=String(index+1).padStart(2,"0");stage.textContent=step.stage;summary.textContent=`${step.detail} · ${step.elapsed_ms} ms`;status.className=step.status;status.textContent=step.status;detail.append(stage,summary);row.append(number,detail,status);fragment.append(row)});document.querySelector("#trace").replaceChildren(fragment)}
function render(result){document.querySelector("#response-text").textContent=result.response;document.querySelector("#intent").textContent=result.intent;document.querySelector("#adapter").textContent=result.adapter;document.querySelector("#human").textContent=result.requires_human?"Required":"Not required";const decision=document.querySelector("#decision");decision.textContent=result.decision;decision.className=`badge ${result.decision}`;const total=result.trace.reduce((sum,step)=>sum+step.elapsed_ms,0);document.querySelector("#elapsed").textContent=`${total} ms`;renderTrace(result.trace)}
async function simulate(event){if(event)event.preventDefault();runButton.disabled=true;runButton.textContent="Simulating…";try{const response=await fetch("/simulate",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message:message.value})});if(!response.ok)throw new Error();render(await response.json())}catch{document.querySelector("#response-text").textContent="Start the FastAPI app and try again."}finally{runButton.disabled=false;runButton.textContent="Simulate request"}}
document.querySelectorAll(".presets button").forEach(button=>button.addEventListener("click",()=>{message.value=button.dataset.message;simulate()}));form.addEventListener("submit",simulate);simulate();
